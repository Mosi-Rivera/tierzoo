const elo_prizes = (current,max) => {
    if (current > max)
    {

    }
    return null;
};

const win_prizes = () => {};

const loss_prizes = () => {};

const game_prizes = () => {};

module.exports = {};